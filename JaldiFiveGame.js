// const user = require("readline").createInterface({
//   input: process.stdin,
//   output: process.stdout,
// });
/* const recordedValues = [];
let jaldiValue;

for (let i = 0; i <= 100; i++) {
  do {
    jaldiValue = Math.floor(Math.random() * 101);
  } while (recordedValues.includes(jaldiValue));

  recordedValues.push(jaldiValue);
  console.log(jaldiValue);
}

console.log(recordedValues);
 */
/* const recordedValues = [];
let jaldiValue;
let delay = 5000; // 5000 milliseconds = 5 seconds


// recursive function

function assignValue(index) {
  if (index > 100) return

  do {
    jaldiValue = Math.floor(Math.random() * 101);
  } while (recordedValues.includes(jaldiValue));

  recordedValues.push(jaldiValue);
  console.log(jaldiValue);

  setTimeout(() => {
    assignValue(index + 1);
  }, delay);
  console.log(recordedValues);
}

assignValue(0);
 */

let jaldiFiveValues=[];

let jaldiValue ;

function genratedValues(){
 
  
}